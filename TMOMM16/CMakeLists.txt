CMAKE_MINIMUM_REQUIRED(VERSION 2.4.8)
SET(CMAKE_COLOR_MAKEFILE ON)
SET(CMAKE_VERBOSE_MAKEFILE ON)
SET(CMAKE_INCLUDE_CURRENT_DIR TRUE)

IF(UNIX AND NOT LINUX_SET)
 ADD_DEFINITIONS(-D LINUX)
ENDIF(UNIX AND NOT LINUX_SET)

INCLUDE_DIRECTORIES(
../tmolib/
.
)

LINK_LIBRARIES (
tmo
)

SET(TMO_SOURCES
 TMOMM16.cpp
 TMOPlugin.cpp
)


SET(TMOWARD_HEADERS
 TMOMM16.h
 TMOPlugin.h
)


add_library( MM16 SHARED ${TMO_SOURCES} )
add_custom_command( TARGET MM16 POST_BUILD 
	COMMAND cp -f libMM16.so ../TMOCmd/TMOMM16.tml
	COMMAND cp -f libMM16.so ../TMOgui/TMOMM16.tml 
	COMMAND cp -f libMM16.so ../TMOMM16.tml 
#	COMMAND rm -f libMM16.so 
	)


